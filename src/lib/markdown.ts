// TODO: Replace with a small MD renderer (e.g., marked or micromark) with safe sanitization.
export function renderMarkdown(md: string): string {
  // For now, just return a fixed placeholder to avoid any unsafe rendering.
  return "<p>[MD would render here]</p>"
}
